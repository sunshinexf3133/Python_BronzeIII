#计算未知个数字的总和
#donesum.py

###不断要求用户输入数字，直到用户输入‘done’结束循环

total = 0
s = raw_input('Enter a number(or done):')
while s != 'done' :
    num = int(s)
    total = total + num
    s = raw_input('Enter a number(or done):')
print('The sum is:' + str(total))

