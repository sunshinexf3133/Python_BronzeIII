#-*-coding:utf-8-*-
#创建python模块
###该模块用于在屏幕上打印形状
#shapes.py

"""A collecting of functions for printing basic shapes.
"""
CHAR = '*'
def rectangle(height,width) :
    """Prints a rectangle."""
    for row in range(height):
        for col in range(width):
            print CHAR,;
        print
def square(side) :
    """Prints a square."""
    rectangle(side,side)
def triangle(height) :
    """Prints a right triangle,"""
    for row in range(height):
        for col in range(1,row + 2):
            print CHAR,;
        print 
    
